# Server
NODE_ENV=development
PORT=3000

# App
APP_NAME=api-starter
APP_VERSION=1.0.0

# Database
# Docker Compose local: docker compose up -d
DATABASE_URL=postgresql://api:secret@localhost:5432/api_dev
